# Nombre del microservicio
spring.application.name: yisas-gpro-results-api

# Configuración Cloud Config
spring.cloud.config.enabled=${CONFIG_ENABLED:true}
spring.cloud.config.uri=${CONFIG_SERVER:http://localhost:8888}
spring.cloud.config.profile=${SPRING_PROFILES_ACTIVE:local}
spring.cloud.config.username=${CONFIG_SERVER_USER:root}
spring.cloud.config.password=${CONFIG_SERVER_PASSWORD:s3cr3t}
spring.cloud.config.label=${CONFIG_SERVER_LABEL:master}
spring.cloud.config.fail-fast=${CONFIG_FAIL_FAST:false}